import React from "react";

const HomePage = () => (
  // wrap our content in a react.fragment instead of <div>
  <React.Fragment>
    <h1>Hello, welcome to my blog!</h1>
    <p>
      This is the blog I create for the purpose of learning web development
      using React.js, Node.js and MongoDB
    </p>
  </React.Fragment>
);

export default HomePage;
